name: Test wf
on:
  push:
    branches: ['main', 'develop']
jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: check branch
        run: |
          echo ${{ github.head_ref }}
          echo "main branch"
